import styled from 'styled-components';

const StyledButton = styled('button')`
    margin: 10px 0;
    width: 100%;
    height: 40px;
    text-align: center;
    cursor: pointer;
`;

export default StyledButton;
